# n8n/live

Актуальные экспорты ЖИВЫХ воркфлоу n8n (без секретов: Supabase-ключ — через `{{ $env.SUPABASE_SERVICE_KEY }}`).
Обновление: `docker exec kote-n8n n8n export:workflow --all --separate --output=/tmp/wf/`.
Историческая справка — в `n8n/reference/`.
